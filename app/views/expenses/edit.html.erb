<% provide(:title, "Edit Expense") %>
<% if @expense.user.id != current_user.id %>
  <h1>Action Not Permitted</h1>
  <h2>For Current User</h2>
<% else %>

  <h1>Editing Expense</h1>

  <%= render 'form', expense: @expense %>
  <div id="left"><a class="btn btn-default" href=" /">MAIN MENU</a> <a class="btn btn-default" href="/expenses">VIEW ALL</a></div>

<% end %>
